export default function isOdd(binRep) {
  // //Assume 'binRep' is a string consisting of 0's and 1's. 
  // let lastDigit = binRep[binRep.length-1];

  // console.log("Last digit of 'binRep' is:  " + lastDigit);

  // if (lastDigit === '0') {
  //   return false; 
  // } else {
  //   return true; 
  // } 
}
